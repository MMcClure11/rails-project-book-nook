<h1>Review for <%= @book.title %> by <%= @review.user.username %> </h1>

<h2>Rating: <%= @review.rating %></h2> 
<h3><%= @review.content %></h3>

<%# if @review.user == current_user %>
<% if owns_resource?(@review)%>
<%= link_to "Edit this Review", edit_book_review_path(@book, @review) if @review.book %>
<% end %>